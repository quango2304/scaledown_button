Scale down the child when click
  
**Example**
```
ScaledownButton(
  onTap: (){},
  scale: 0.1,
  child: Icon(
    Icons.mail_outline,
    color: Colors.grey,
    size: 25,
  ),
)
```

result:

![](demo.gif)
